interface:
  display_name: "Second Brain Query"
  short_description: "Query the wiki with evidence and currentness checks"
  default_prompt: "Use $second-brain-query to answer from the wiki with evidence labels, cutoff dates, and citations."
